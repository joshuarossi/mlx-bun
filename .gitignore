node_modules
*.local
.env
.npmrc
.DS_Store
tmp/
project-review.html
# generated reports / benchmark artifacts (regenerated, not source).
# RESULTS.md is the curated, deliberately-tracked record — see benchmarks/RESULTS.md.
*-report-*.html
benchmarks-h2h-*
benchmarks-e4b-flag-matrix-*.json
benchmarks/lab-verification*.json
reports/
runs/
dist/
.claude/
dist-native/
dist-release/
# investigation scratch — regenerable from scripts/experiments/*; the write-ups
# (docs/investigations/*.md) stay tracked, the raw run data does not.
docs/investigations/hlg-figs/
docs/investigations/hlg-runs/*.log
docs/investigations/hlg-runs/*.json
docs/investigations/curve-runs/
docs/investigations/*.html
# trained adapters/checkpoints (weights, not source) + training run logs
adapters/
train.log
*.train.log
